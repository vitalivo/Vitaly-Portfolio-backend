[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/api/health/"  # ✅ ИСПРАВЛЕНО!
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[env]
DJANGO_SETTINGS_MODULE = "config.settings.production"
PYTHONPATH = "/app"
